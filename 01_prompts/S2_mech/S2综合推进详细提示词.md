# S2 → AI 提示词：57BLF02 方案的力学、电机、机械子系统与工程交付综合推进

> 提示词类型：S2 综合执行请求  
> 目标：围绕当前冻结的 57BLF02 方案，输出可直接落地到课程设计仓库中的数学推导、输入输出定义、人工核查、Simulink 机械子系统方案、Rs/Ls 实测方案、机械侧损耗分析、BOM 与关键器件选型。  
> 归档路径：`01_prompts/S2_mech/S2综合推进详细提示词.md`

---

## 1. 背景说明

我当前担任课程设计项目中的 S2 力学与电机层工程师，项目主题为差速驱动轮电机伺服控制系统。团队采用 AI 协作方式推进设计，要求所有方案必须强调：

1. 数学推导严谨；
2. 参数来源可追溯；
3. 仿真步骤可复现；
4. 对 Simulink 建模具有直接指导意义；
5. 输出结果可直接交接给 S3、S4、S5。

当前项目已经从旧的 42BLF02 方案切换到 57BLF02 方案，`00_frozen/params.md` 与 `04_simulink/init_params.m` 已同步。当前顶层依据文件为 S1 的 v1.5 文档《构架方案3（被控对象修正）》，现在需要你基于这个冻结方案，从 S2 视角输出一份完整、严谨、可执行、可交接的技术结果。

---

## 2. 你的角色

你是一名顶级电机驱动与机电系统建模专家，同时精通：

1. 轮式移动机器人力学建模；
2. BLDC 电机选型与参数解释；
3. MATLAB/Simulink 机械负载子系统设计；
4. 工程测试方法设计；
5. 校验笔记与报告级文档写作。

你的输出必须符合“课程设计可交付”的标准，而不是停留在泛泛解释层面。

---

## 2.1 S2 在项目总任务拆解中的职责

根据顶层架构文档第 1.3 节任务拆解与分工，S2 需要完成的任务不止机械推导，还包括与 S3、S4、S5 的协作接口。请你在回答中显式覆盖以下任务：

| 任务模块 | 分值 | S2 责任 |
|------|------|---------|
| ① 力学分析与传动方案 | 2 | **S2 主责**，完成负载折算、传动方案、电机/减速机选型与论证 |
| ③ 效率与损耗分析 | 2 | **S2 + S3**，其中 S2 主责机械侧损耗、滚动阻力损耗、减速机损耗与系统效率链中的机械部分 |
| ④ 成本与工程可实施性 | 2 | **S2 主责**，完成电机/减速机/轮组/传感器相关 BOM 与工程实现分析 |
| ⑤ 关键器件选型 | 2 | **S2 主责电机/传动/传感器部分**，需输出真实可采购器件型号、参数与选型理由 |
| ⑥ 建模/控制器/仿真 | 9 | **S4 主责**，但 S2 需交付 `mechanical_load.slx` 所需的数学关系、输入输出接口与验收标准 |
| ⑦ AI 工作流归档 | — | S2 需要输出可直接归档到 prompt/response/check 三层目录的内容 |

因此，你的回答必须体现：S2 不是只负责公式推导，而是负责把 L1 + L2 层的输入契约、输出契约、工程实现和交接材料一起做完整。

---

## 3. 当前冻结参数与约束

以下参数已经冻结，不允许你自行修改：

| 参数 | 数值 | 单位 | 说明 |
|------|------|------|------|
| 整机质量 $m$ | 50 | kg | 满载 |
| 驱动轮半径 $r$ | 0.08 | m | 固定 |
| 滚动阻力系数 $f$ | 0.04 | - | 固定 |
| 最大坡度 $\alpha_{max}$ | 5 | ° | 最恶劣上坡工况 |
| 机械效率 $\eta_m$ | 0.90 | - | 固定 |
| 低速轮速 $n_{low}$ | 60 | rpm | 靠站 |
| 高速轮速 $n_{high}$ | 240 | rpm | 巡航 |
| 母线电压 $U_{dc}$ | 48 | V | 题设固定 |
| 行星减速比 $i$ | 12.5 | - | 由 3000/240 得到 |
| 电机型号 | 57BLF02 | - | 当前冻结型号 |
| 电机额定电压 $U_N$ | 24 | V | 通过 PWM 降压使用 |
| 电机额定功率 $P_N$ | 125 | W | 冻结值 |
| 电机额定转速 $n_N$ | 3000 | rpm | 冻结值 |
| 电机额定电流 $I_N$ | 7.8 | A | 冻结值 |
| 电机峰值电流 $I_{peak}$ | 23.5 | A | 电机能力上限 |
| 电机额定转矩 $T_N$ | 0.40 | N·m | 冻结值 |
| 电机峰值转矩 $T_{peak}$ | 1.20 | N·m | 冻结值 |
| 力矩常数 $K_t$ | 0.066 | N·m/A | 冻结值 |
| 反电势常数 $K_e$ | 6.3 | V/krpm | 冻结值 |
| 转子惯量 $J_m$ | 1.7×10⁻⁵ | kg·m² | 冻结值 |
| 相电阻 $R_s$ | 0.30 | Ω | 当前工程冻结值，待实测 |
| 相电感 $L_s$ | 0.75 | mH | 当前工程冻结值，待实测 |
| 控制器电流限幅 | 12 | A | 控制器限幅，不等于电机峰值 |

补充说明：

1. 机械折算的已知冻结结果包括：
   - $T_{L,m,平}=0.0698$ N·m
   - $T_{L,m,坡}=0.2215$ N·m
   - $J_{eq}=5.60\times10^{-4}$ kg·m²
   - $T_{m,peak}=0.4414$ N·m
   - $P_{m,稳态}=69.6$ W
2. 已完成一轮人工核查，结果表明全部关键量与冻结值的误差均远小于 1%。

3. 顶层架构文档已经明确：
   - S2 的 L1 输出契约为阻力力矩 $T_L(v,\alpha,m)$、折算惯量 $J_{eq}$、工况时序表；
   - S2 的 L2 输出契约为电机型号、减速比 $i$、电机铭牌参数 $(R_s,L_s,K_e,K_t,J_m,p)$；
   - S2 还需要向 S4 交付 `mechanical_load.slx` 所依据的接口定义与验收标准。

---

## 4. 任务要求

请你围绕 S2 职责，完整完成以下 7 大任务，并按章节结构输出。

### 任务 A：数学模型推导

请从 S2 视角，严谨推导差速驱动轮电机系统的机械与电机侧数学模型，至少包括：

1. 轮侧角速度、线速度推导；
2. 平路单轮阻力与轮侧负载力矩推导；
3. 坡道法向压力、重力切向分量、总切向力、轮侧负载力矩推导；
4. 从轮侧到电机轴侧的力矩折算推导；
5. 车身平动惯量、车轮惯量折算到电机轴侧的推导；
6. 总折算惯量 $J_{eq}$ 的形成过程；
7. 在上升时间约束 $t_r \le 0.8$ s 下，动态加速转矩、峰值转矩需求与稳态功率需求的推导；
8. 从负载需求反证 57BLF02 选型是否合理。

要求：

1. 每一步都写清楚公式、代入数值和单位；
2. 明确说明每个结果的物理意义；
3. 不能跳步，不能只给结论。

### 任务 B：电机与机械子系统输入输出重新分析

请根据 S1 v1.5 架构文档，重新分析“电机本体”和“mechanical_load 子系统”的输入输出，不能把两者混为一谈。要求你至少完成：

1. 说明在总装模型中，**BLDC 电机本体**的输入有哪些，输出有哪些；
2. 说明在总装模型中，**mechanical_load 子系统**的输入有哪些，输出有哪些；
3. 说明哪些输入来自 S3 功率级，哪些输入来自 S4 控制器，哪些输入由 S2 自己定义；
4. 说明哪些输出要交给 S4 做反馈与机械方程，哪些输出只是调试观测量；
5. 对输入输出给出单位、物理意义、信号方向、在总装模型中的连接位置。

你必须至少覆盖以下两个层面：

#### 层面 1：BLDC 电机本体 I/O

请分析并定义：

1. 电机电气输入：来自逆变器的相电压/等效电压；
2. 电机机械输入：外部负载力矩 $T_L$；
3. 电机输出：
   - 转速 $\omega_m$
   - 电角度 $\theta_e$ 或 Hall 信号
   - 相电流 $i_a,i_b,i_c$ 或等效电流反馈
   - 电磁转矩 $T_e$

#### 层面 2：mechanical_load 子系统 I/O

请分析并定义：

1. 输入：
   - `omega_m`
   - `slope_deg`
   - `mass_kg`
2. 输出：
   - `T_L_m`
   - `T_L_w`
3. 当前 v1 版本中 `omega_m` 是否真正参与计算；
4. 后续如何扩展速度相关阻尼项 $B\omega_m$。

### 任务 C：人工核算与校验结论

请根据当前冻结方案，输出一份面向人工校核的结构化结果，至少包括：

1. 哪些量最值得人工重点复核；
2. 为什么这几个量是关键节点；
3. 若脚本输出与冻结值误差 <1%，应如何给出工程结论；
4. 若误差超标，排查顺序应该是什么。

请把人工核算重点锁定在以下量上：

1. $T_{L,m,坡}$
2. $J_{eq}$
3. $T_{加速}$
4. $T_{m,peak}$
5. $P_{m,稳态}$

输出时请给出一张“人工核查表”，列出：符号、公式、冻结值、人工验算值、允许误差、判定规则。

### 任务 D：mechanical_load.slx 子系统设计

请为 Simulink 中的 `mechanical_load.slx` 生成完整设计方案，要求可直接指导建模。至少包括：

1. 子系统输入输出端口定义：
   - `omega_m`，单位 rad/s
   - `slope_deg`，单位 °
   - `mass_kg`，单位 kg
   - `T_L_m`，单位 N·m
   - `T_L_w`，单位 N·m
2. 子系统内部数学关系；
3. 推荐使用 MATLAB Function Block 还是基础公式块，并说明理由；
4. 给出 MATLAB Function Block 的完整实现代码；
5. 给出与 S4 机械方程对接时的接口说明；
6. 给出两组验收工况：
   - 平路 50kg，应得到 `T_L_m ≈ 0.0698 N·m`
   - 5° 坡道 50kg，应得到 `T_L_m ≈ 0.2215 N·m`
7. 明确说明当前 v1 版本中 `omega_m` 是否参与负载力矩计算，以及未来如何扩展出黏性阻尼项 $B\omega_m$；
8. 给出该子系统对 S4 的交付口径，说明 S4 接入时应把 `T_L_m` 接到哪里。

输出时要体现“直接可建模”，不能只给概念说明。

### 任务 E：Rs、Ls 实测方案

由于当前公开资料未稳定找到 57BLF02 的 Rs、Ls，请给出一套工程上可执行的实测方案，用于后续校准 `params.md` 与 `init_params.m`。至少包括：

1. 线-线电阻法测量 Rs：
   - 仪器
   - 接线方式
   - 测试步骤
   - 温度影响
   - 多次测量取平均规则
2. 锁轴阶跃响应法测量 Ls：
   - 仪器
   - 接线方式
   - 示波器采样内容
   - 如何从 $\tau_e$ 反算 $L_s$
3. LCR 表辅助测量法；
4. 结果如何判定是否需要修改冻结参数；
5. 如果 Rs、Ls 变化超过 5%，应联动影响哪些控制参数和仿真模块。

请同时给出一张“实测记录模板表”，供后续现场手填。

### 任务 F：机械侧效率与损耗分析

请补充 S2 在任务模块③中应负责的内容，至少包括：

1. 平路与坡道工况下的滚动阻力功率损耗；
2. 减速机效率带来的机械功率损失；
3. 从轮侧输出到电机轴输入的功率链；
4. 哪些损耗属于 S2 负责，哪些应明确交给 S3 负责；
5. 给出适合写入课程设计报告 3.3 节的表格化结论。

### 任务 G：成本、工程可实施性与关键器件选型

请补充 S2 在任务模块④和⑤中应负责的内容，至少包括：

1. 电机、减速机、轮组、霍尔传感器、安装件的关键器件选型表；
2. 每个器件的关键参数、选型理由、可采购性、参考价格；
3. 双轮驱动场景下的基础 BOM 汇总；
4. 工程实施时最容易踩的坑；
5. 哪些器件参数会直接影响 `init_params.m` 和 Simulink 建模精度。

---

## 5. 输出格式要求

请按以下固定结构输出：

1. `一、S2 职责边界与任务映射`
2. `二、57BLF02 方案适用性结论`
3. `三、数学模型推导`
4. `四、电机本体与 mechanical_load 子系统输入输出定义`
5. `五、人工核算重点与校验表`
6. `六、mechanical_load.slx 设计方案`
7. `七、Rs/Ls 实测方案`
8. `八、机械侧效率与损耗分析`
9. `九、成本、工程可实施性与关键器件选型`
10. `十、对 S3/S4/S5 的交接要点`

其中：

1. 公式必须规范；
2. 表格必须完整；
3. 代码必须可直接复制；
4. 用词必须工程化，不要写空泛描述；
5. 如果某处存在工程假设，要显式标注“当前冻结假设”；
6. 涉及输入输出时，必须分别标明“信号名 / 单位 / 来源 / 去向 / 是否必需”。

---

## 6. 特别约束

1. 不允许擅自把电机改回 42BLF02。
2. 不允许修改冻结参数。
3. 不允许跳过数学推导直接下结论。
4. 不允许把 Rs、Ls 说成“已确认”，因为当前状态是“工程冻结值，待实测”。
5. 输出必须面向 MATLAB R2023a 与课程设计场景，优先成熟、稳定、可复现方案。
6. 必须与 S1 v1.5 顶层文档保持口径一致，特别是：57BLF02、i=12.5、24V 电机 + 48V 母线 PWM 降压、MO/SO 控制背景。

---

## 7. 最终目标

你的输出应达到以下效果：

1. 我可以直接把“数学推导”部分写进 S2 响应文档；
2. 我可以直接据此完善人工校验笔记；
3. 我可以照着你的说明去搭 `mechanical_load.slx`；
4. 我可以明确区分“电机本体 I/O”和“mechanical_load 子系统 I/O”；
5. 我可以把机械侧损耗、BOM、关键器件选型直接写进报告与交接材料；
6. 我可以把 Rs、Ls 的实测方案直接交给后续执行者；
7. S3、S4、S5 读完之后，不需要再反复追问 S2 的假设、接口和责任边界。

请按上述结构一次性输出完整结果。